@model IEnumerable<CO.Web.Models.ProductViewModel>

@{
    ViewData["Title"] = "GetList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <br /><br />
            <a href="#" class="addfld btn btn-success universalWidth">Create Product</a>
            <br /><br /> <br />
        </div>
        <div class="card">
            <div class="body">
                <div class="table-responsive">
                    <table id="myTable" class="table table-bordered table-striped table-hover js-basic-example dataTable">
                        <thead>
                            <tr>

                                <th>Name</th>
                                <th>Value</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {

                                <tr>
                                    @Html.HiddenFor(modelItem => item.Id)
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Value)
                                    </td>
                                    <td>
                                        <a href="#" data-id="@item.Id" class="editProduct btn btn-success universalWidth">Edit Product</a>
                                        <a href="#" data-id="@item.Id" class="deleteproduct btn btn-success universalWidth">Delete Product</a>


                                    </td>
                                   
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script type="text/javascript">
       
        $('.editProduct').click(function () {
            var id = $(this).data('id');
            var url = "/WebPage/GetProduct?id=" + id;
            window.location.href = url;
        })

        $('.deleteproduct').click(function () {
            var id = $(this).data('id');
            var url = "/Field/AddField?id=" + id;
            window.location.href = url;
        })

        //var baseUrl = '@Url.Action("/GetForm")';
        $(document).ready(function () {
            var table = $('#myTable').DataTable();

            //$('#myTable tbody').on('click', 'td', function () {
            //    if ($(this).index() == 5) {
            //        return;
            //    }
            //    var id = $('#item_Id').val();
            //    location.href = baseUrl + '?id=' + id;
            //});
        });

    </script>
}

